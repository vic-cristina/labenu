const num = Number(prompt("Insira um número"));
console.log(`${num} / 2 = ${num / 2} \n com resto ${num % 2}`);

// Caso o número seja par, o módulo (num % 2) sempre será === 0.
// Caso o número seja ímpar, o módulo sempre será > 0.
